let nomeCompleto = 'Marcos Barros';

function transformaEmMaiusculo() {
    console.log(` Assim fica o nome "${nomeCompleto}" em letras maiúsculas:\n ${nomeCompleto.toLocaleUpperCase()}`);
}

transformaEmMaiusculo();